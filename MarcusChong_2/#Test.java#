import java.util.Scanner;
import java.util.Random;

public class Test
{
	public static void main(String[] args)
	{
		Scanner scan = new Scanner(System.in);
		Random roll = new Random();
		int dice1, dice2, total, total2 = 0, point, count = 1, myWins = 0, myLosses = 0;
		while( count != 11 )
		{
			dice1 = roll.nextInt(6) + 1;
			dice2 = roll.nextInt(6) + 1;	
			total = dice1 + dice2;
			//System.out.println( dice1 + " " + dice2);
			if( total = 7 || total = 11)
			{
				myWins++;
			}
			
			else if( total=2 || total = 3 || total = 12)
			{
				myLosess++;
			}
			count++;
		}
		//double percent = myWins/(myWins+myLosses)*100;
		//System.out.println("# of Wins: " + myWins);
		//System.out.println("# of Losses: " + myLosses);
		//System.out.println("Your probability of winnning is: " + percent + "%.");
		
		
	}
}